CREATE TABLE [dbo].[AutoConnectionModelRoles] (
    [Key]     INT           NOT NULL,
    [RunName] NVARCHAR (64) NULL,
    [Items]   INT           NULL,
    PRIMARY KEY CLUSTERED ([Key] ASC)
);

